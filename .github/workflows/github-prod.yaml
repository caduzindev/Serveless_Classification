name: CI
on: [push]

jobs:
  node_docker:
    runs-on: ubuntu-latest
    name: Test Job
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: Tests Units
        uses: actions/Serveless_Classification@v1
